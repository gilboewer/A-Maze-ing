[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
readme = "README.md"

[tool.setuptools.package-dir]
my_package = "src/mazegen"
